.DS_Store
.claude/settings.local.json

# 프로덕션 자격증명 (절대 git에 올리지 않음)
/PRODUCTION_CREDENTIALS.md

# 2019년 구 홈페이지 원본 (새 사이트로 콘텐츠 이관 완료, 로컬 참고용으로만 보관)
/iv_h/

# 강의 자료 원본 (2020~2022 스타트업 대상 강의).
# 이 저장소는 PUBLIC이다. 자료 안에 실명 사례와 특정 기업 투자 이력이 있어
# 커밋하면 그대로 공개된다. 인사이트 글로 옮기는 작업은 끝났고 원본은
# 로컬 참고용으로만 둔다.
/*.pptx

# FastAPI 백엔드 (2026-08-29 제거).
# 프론트엔드가 한 번도 호출하지 않는 죽은 코드였고, seed.py 에 팀원 실명과
# 고객사 명단이 들어 있어 공개 리포에 둘 수 없었다. 이력에서도 지웠다.
# 로컬 백업: P/iv-backups/20260829/
# 되살릴 일이 있어도 seed.py 의 TEAM_MEMBERS·CLIENTS 는 복구하지 않는다.
/backend/
